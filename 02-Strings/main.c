#include<string.h>
#include "StringOperations.h"

int main()
{
    
    char str2[]="HelloWordlabcHelloWorld";
    // printf("%d",length(str));
    // printf("%s",str);
    // UpperCase(str);
    // printf("%s",str);
    // LowerCase(str);
    // printf("\n\n\nn%s",str);
    // Append(str2,str);
    // printf("\n\n\nn%s",str);
    // Compare(str,str2);
    // Reverse(str2);

  



}